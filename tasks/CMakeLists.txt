# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(modem_positionTaskLib)
ADD_LIBRARY(${MODEM_POSITION_TASKLIB_NAME} SHARED 
    ${MODEM_POSITION_TASKLIB_SOURCES})
add_dependencies(${MODEM_POSITION_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${MODEM_POSITION_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${MODEM_POSITION_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${MODEM_POSITION_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${MODEM_POSITION_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${MODEM_POSITION_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${MODEM_POSITION_TASKLIB_HEADERS}
    DESTINATION include/orocos/modem_position)

